post:
  tags:
    - "TimeRegistration"
  operationId: "addTimePeriodRegistrationForUser"
  description: "Register time period defined by a date range. Will be converted into single time registrations for each day in the range."
  requestBody:
    description: "TaskId along with Start and end date of the period (both included). Description is optional"
    required: true
    content:
      application/json:
        schema:
          $ref: "../schemas/user/TimePeriodRegistrationRequest.yaml"
  responses:
    "201":
      description: "Time registrations within period added successfully"
      content:
        application/json:
          schema:
            type: "array"
            items:
              $ref: "../schemas/DefaultCreateResponse.yaml"
          examples:
            success:
              value: [
                {
                  "id": 12345,
                  "status": "The time registration was created successfully"
                },
                {
                  "id": 12346,
                  "status": "The time registration was created successfully"
                }
              ]
    "400":
      description: "One or more time registrations failed. If any registration fails, none of the registrations will be saved"
      content:
        text/plain:
          schema:
            type: "string"
    "401":
      $ref: "../responses/UnauthorizedError.yaml"
