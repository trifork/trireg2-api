get:
  tags:
    - "User"
  operationId: "getPrivilegesForUser"
  description: "Gets the privileges for the user"
  responses:
    "200":
      description: "Successfully fetched the user privileges"
      content:
        application/json:
          schema:
            $ref: "../schemas/user/UserPrivileges.yaml"
    "401":
      $ref: "../responses/UnauthorizedError.yaml"