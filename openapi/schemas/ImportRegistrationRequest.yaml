type: "array"
items:
  required: ["userId", "taskId", "date", "duration", "tags"]
  properties:
    userId:
      type: "string"
    taskId:
      type: "string"
    date:
      type: "string"
      format: "date"
    duration:
      $ref: Duration.yaml
    tags:
      type: array
      items:
        $ref: Tag.yaml
