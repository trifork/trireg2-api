type: "array"
items:
  required: ["userId", "taskId", "date", "duration", "tags"]
  properties:
    userId:
      type: "string"
    taskId:
      type: "string"
    date:
      type: "string"
      format: "date"
    duration:
      $ref: Duration.yaml
    tags:
      $ref: Tag.yaml
