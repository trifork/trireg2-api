type: "object"
required: ["taskId", "date", "from", "to", "km", "status"]
properties:
  taskId:
    type: "integer"
    format: "int64"
  date:
    type: "string"
    format: "date"
  from:
    type: "string"
  to:
    type: "string"
  km:
    type: "integer"
  status:
    $ref: KmRegistrationStatus.yaml