type: "object"
required: ["kmRegistrationId", "taskId", "date", "from", "to", "purpose", "km", "status"]
properties:
  kmRegistrationId:
    type: "integer"
    format: "int64"
  taskId:
    type: "integer"
    format: "int64"
  date:
    type: "string"
    format: "date"
  from:
    type: "string"
  to:
    type: "string"
  km:
    type: "integer"
  purpose:
    type: "string"
  status:
    $ref: KmRegistrationStatus.yaml