type: "object"
properties:
  date:
    type: "string"
    format: "date"
  duration:
    $ref: ../Duration.yaml
  status:
    type: "string"
    description: "Describes the status of the time registration"
    nullable: true
    enum:
      - "PENDING"
      - "VALID"
      - "INVALID"
      - null
    default: null
        #type: "string"
        #$ref: ../Status.yaml