type: "array"
items:
  required: ["timeRegistrationId", "status"]
  properties:
    timeRegistrationId:
      type: "integer"
      format: "int64"
    status:
      $ref: "ImportTimeRegistrationStatus.yaml"
    message:
      description: "Provides a description of the import status"
      type: "string"
