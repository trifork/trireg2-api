type: "array"
items:
  required: ["timeRegistrationId", "status"]
  properties:
    timeRegistrationId:
      type: "integer"
      format: "int64"
    status:
      type: "string"
      description: "Describes the status of the registration (SUCCESS, PENDING, FAILED)"
    message:
      description: "Provides a description of the status"
      type: "string"
