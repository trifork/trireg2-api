type: "object"
required: ["taskId", "name"]
properties:
  taskId:
    type: "integer"
    format: "int64"
  name:
    type: "string"
    description: "The name of the task"
  description:
    type: "string"
    description: "A description of the task"
